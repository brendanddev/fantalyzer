[project]
name = "alert-dispatcher"
version = "0.1.0"
description = "Delivers fire-worthy scored events as push notifications via ntfy.sh."
requires-python = ">=3.12"
dependencies = [
    "psycopg2-binary>=2.9",
    "requests>=2.32",
    "schedule>=1.2",
]

[tool.setuptools]
py-modules = ["main"]
