[project]
name = "scoring-engine"
version = "0.1.0"
description = "Fuses raw ingested signals with curated handcuff data into scored, actionable events."
requires-python = ">=3.12"
dependencies = [
    "psycopg2-binary>=2.9",
    "requests>=2.32",
    "schedule>=1.2",
]

[tool.setuptools]
py-modules = ["main"]
